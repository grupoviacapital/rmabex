---
description: Inicia uma nova spec a partir de uma ideia (estágio 1→3 do loop spec-driven)
argument-hint: <ideia da feature>
---

Inicie o fluxo de autoria de spec para a ideia abaixo, usando o skill `create-spec`.

Ideia: $ARGUMENTS

Comece pelo Estágio 1 (requisitos, via subagente `spec-writer`). Determine o próximo número de spec olhando `vault/10-specs/`. Pare para aprovação humana ao fim de cada estágio - não gere requirements, design e tasks de uma vez.
