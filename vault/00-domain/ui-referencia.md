# Referência de UI - Design Tokens & Layout

> **Fonte durável da identidade visual.** Este arquivo é **preenchido pelo comando `/identidade`** (agente `brand-designer`), a partir da pasta de identidade que você apontar, ou da opção escolhida entre as geradas. Toda spec de front-end referencia esta nota.

## Status

`Pendente` - rode `/identidade` para preencher.

## O que vai aqui (após `/identidade`)

- **Tema**: claro/escuro, tom geral.
- **Tipografia**: fontes por papel (display, corpo, dados) com pesos.
- **Paleta (tokens)**: tabela `--token | hex | uso`.
- **Estrutura da página**: seções em ordem.
- **Componentes**: inventário a recriar.

*(Stub do template. Não edite à mão antes de rodar `/identidade`; depois, este vira a fonte de verdade dos tokens.)*
